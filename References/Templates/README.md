# 情境模板库

此目录用于存放用户保存的"情境模板"。

## 使用方法

- **保存模板**：当你满意某次 AI 输出的格式时，对 AI 说"存为模板"或"保存这个格式"，AI 会自动将输出骨架存入此目录。
- **调用模板**：下次遇到同类任务，直接说"按 [模板名] 出一份"，AI 会自动加载模板并结合当前 Boss Persona 生成。

## 模板结构

每个模板文件包含：
- 输出的结构骨架（标题层级、分段逻辑）
- 数据源指引（从哪些文件/输入中拉取信息）
- 关联的 Boss（如适用）

模板不包含具体业务内容，只保留格式框架。
